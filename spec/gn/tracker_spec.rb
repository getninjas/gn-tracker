require 'spec_helper'

describe Gn::Tracker do
  it 'has a version number' do
    expect(Gn::Tracker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
